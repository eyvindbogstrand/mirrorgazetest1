<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.test11mirrorpartnereyes">

    <!-- Kamera-tillatelse (paakrevd for oeyestyring) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Kamera er naa paakrevd (for head pose estimation) -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.front" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test11mirrorpartnereyes"
        tools:targetApi="31">

        <!-- Eyestest1-aktivitet (beholdes fra eksisterende) -->
        <activity
            android:name=".Eyestest1"
            android:exported="true" />

        <!-- MainActivity - Hovedaktivitet med staving + oeyestyring -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:keepScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
